var fs = require('fs');
var csv = require('csv');
var ENTITYFILE = require("papers.json")
var SESSIONFILE = require("sessions.json")
var SCHEDULEFILE = require("schedule.json")

console.log(ENTITYFILE);